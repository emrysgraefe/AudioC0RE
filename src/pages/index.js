import React from 'react'
import SiteLayout from '../layouts/SiteLayout'

const HomePage = props => (
  <SiteLayout>
    <h1>Home</h1>
    <p>This website is for AudioC0RE, which is an awesome thing that does stuff.</p>
  </SiteLayout>
)

export default HomePage