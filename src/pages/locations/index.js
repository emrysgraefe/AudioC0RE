import React from 'react'
import SiteLayout from '../../layouts/SiteLayout'

const LocationsPage = props => (
  <SiteLayout>
    <h1>Locations</h1>
    <p>We have three locations now, but we're expanding!</p>
  </SiteLayout>
)

export default LocationsPage